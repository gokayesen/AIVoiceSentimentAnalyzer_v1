# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Session-scoped storage (uploaded audio + intermediates) — never committed
/storage/*
!/storage/.gitkeep

# Node (frontend, Story 2.1+)
node_modules/
dist/

# OS
.DS_Store

# Claude Code runtime state (session-local, not project source)
.claude/scheduled_tasks.lock
